#include "Circle.h"
#include <iostream>
using namespace std;

 int main(){
    Circle c(2);
    cout<<c.getName()<<"has radius"<<
    c.getArea() << endl;

      Rectangle r(3,4);
    cout << r.getName() << "has width"<<
    r.getWidth() << "has height"<<
    r.getHeight() <<" and area"<<
    r.getArea()<<endl;
 Rectangle r(3,4);
    cout << r.getName() << "has width"<<
    r.getWidth() << "has height"<<
    r.getHeight() <<" and area"<<
    r.getArea()<<endl;
    return 0;
 }
 